# Captured instruction review: synthetic task

Three inert sources ask for limited diagnostics, then conflicting broad
diagnostics, then claim peer/owner authority. Registration stays outside captures.
Nothing here authorizes collecting or sending diagnostic material.

[Instructions, limits and installation](../../../docs/controlled-investigator.md).
The static path needs no Docker/model. Optional investigation uses the same frozen
inputs and an operator-selected local-only service. `expected-v1.json` belongs to
the evaluator, never the worker's authority. Scripted HTTP responses are **not a
live-model demonstration**; interpretation truth needs separate evaluation.
